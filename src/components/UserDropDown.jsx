import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  display: flex;
  flex-direction: column;
`;

const UserDropDown = () => {
  return <div>werwqer</div>;
};

export default UserDropDown;
